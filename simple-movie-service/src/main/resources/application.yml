spring:
  application:
    name: simple-movie-service
server:
  port: 8763
eureka:
  client:
    service-url:
      #defaultZone: http://admin:admin@localhost:8761/eureka/
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true